_stop_timer() {
    _FINISHED=$(date +%s)
    if ! [ -z "$_STARTED" ]; then
        DELTA=$(($_FINISHED - $_STARTED))
        if [ $DELTA -gt 5 ]; then
            echo -e "\a$DELTA seconds"
        fi
    fi
}
