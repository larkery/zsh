s () {
  # maybe run file if the file is binary
  # and pager if it is pageable
  if [ -z "$1" ]; then
     ls -l --color=auto
  elif [ -d "$1" ]; then
     ls -l --color=auto -- "$1"
  else
     cat -- "$1"
  fi
}