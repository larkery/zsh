_my_files () {
    _named_directories
    values=(${(f)"$(histdb-frecent-dirs $PREFIX)"})
    local expl
    _tags recent-directories
    _requested recent-directories expl 'recent directory' compadd -U -a values
}