_set_title () {
    name="$1"

    if [[ -n "$terminfo[fsl]" ]] && [[ -n "$terminfo[tsl]" ]]; then
        echoti tsl
        if [[ -z "$name" ]]; then
           print -Pn "zsh: %~"
        else
           print -n "$name"
        fi

        echoti fsl
    fi
}

_set_title
